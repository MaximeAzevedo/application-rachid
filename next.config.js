/** @type {import('next').NextConfig} */
const nextConfig = {
  // Turbopack disabled due to FATAL build errors
  // experimental: {
  //   turbopack: {
  //     root: '.'
  //   }
  // }
};

module.exports = nextConfig; 